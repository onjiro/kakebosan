version: 2
jobs:
  build:
    docker:
      - image: elixir:1.5.2
      - image: postgres:9.5.9
        environment:
          POSTGRES_USER: ubuntu
    steps:
      - checkout
      - run: mix local.hex --force
      - run: mix local.rebar
      - run: mix deps.get
      - run: mix ecto.create
      - run: mix test
